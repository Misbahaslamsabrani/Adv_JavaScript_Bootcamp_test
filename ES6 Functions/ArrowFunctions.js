/* //ES5
var obj = function(id, name){
    return {
        id: id,
        name, name
    }
}
console.log(obj(1,"rehan"))

//ES6
var obj1 = (id, name) => ({id, name})
console.log(obj1(2, "ahmed")) */

/* const smartPhones = [
    { name:'iphone', price:649 },
    { name:'Galaxy S6', price:576 },
    { name:'Galaxy Note 5', price:489 }
  ];

   //ES5
  var prices = smartPhones.map(function(phone){
      return phone.price
  })
  console.log(prices)

  //ES6
  var prices = smartPhones.map((phone) => phone.price)
  console.log(prices) */

  

